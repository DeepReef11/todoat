# This is a list of needed features that may not have been documented in other dev files
Make sure these exists.

## Sample config

A sample config file that contains all backend config and options in the config file. This file gets copied in user folder. It may have inline comments to briefly explain some features.

## Nextcloud docker container 

The integration testing server for nexcloud should be a docker container that is ready to receive tasks. It currently land on a nextcloud page that prompt to create admin user and nextcloud setups. It should have everything setup. See .env file to get username, password.

